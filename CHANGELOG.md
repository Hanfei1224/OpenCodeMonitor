# Changelog

本项目的所有重要变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.1] - 2026-08-13

### Changed
- 右上角「置顶 / 穿透」由文字标签改为**图标按钮**（内联 SVG：图钉 = 置顶、电脑鼠标 = 穿透），
  开启时变绿、关闭时变灰，保持原有开关状态指示。
- 视觉细节调整：
  - 「重置倒计时 / 上次刷新 / 响应时间」文字颜色改为与「已用金额」一致（统一亮色，去掉已用的透明度差异）。
  - 环中心百分比底色由椭圆改为**正圆形**。
  - 环下方「标签 / 重置 / 已用」三行间距加大，避免底色互相粘连。
  - 上述字段字号各放大一号，窗口高度相应加高，避免底部内容被裁剪。

## [1.1.0] - 2026-08-13

### Added
- 右下角显示接口**响应耗时**（毫秒），每次刷新实时更新（`fetch_usage` 用 `time.monotonic()` 计时）。
- **低透明度文字底色**：背景透明度低于 60% 时，为标题、置顶/穿透标签、环中心百分比、
  标签/重置倒计时/已用金额、底部刷新时间与响应耗时等所有文字控件自动垫半透明深色底色
  （圆角矩形 / 圆形），文字可读且不完全遮挡下层内容。
- 安装包文件名加入版本号，命名规则统一为 `OpencodeMonitor-Setup-<版本号>.exe`
  （版本号以 `installer.iss` 的 `MyAppVersion` 为唯一来源）。
- 新增 `README.md` 与 `CHANGELOG.md`。

### Changed
- 版本号从 `1.0.0` 升级为 `1.1.0`（`installer.iss` + `index.html`）。

### Fixed
- 窗口加入 `WS_EX_TOOLWINDOW` 并从任务栏 / Alt+Tab 隐藏，仅保留托盘图标；
  同时清除 WinForms 自动设置的 `WS_EX_APPWINDOW`（0x40000），避免其抵消工具窗口样式、
  导致窗口仍出现在任务栏（实测 exstyle 0xD00A8 → 0x900A8）。
- 环下方文字改为 `width:fit-content` + 水平居中，使底色紧贴文字、上下左右居中。

## [1.0.0] - 2026-08-12

### Added
- 无边框、置顶、可鼠标穿透的桌面磁贴，基于 pywebview(WebView2) + pystray。
- 三个 SVG 环形图：**5 小时 / 本周 / 本月** 用量百分比（官方接口 `opencode.ai/zen/go/v1/usage`）。
- 每个环显示重置倒计时（rolling 精确到分钟，weekly/monthly 精确到小时）。
- **已用美元换算**：`已用$ = percent/100 × 官方配额`（5h=$12、周=$30、月=$60），
  在环下方以 `已用 $X.XX / $Y` 显示。
- 内嵌 API Key 填写面板：未配置 / Key 无效时自动弹出，保存后立即重连（含输入框可点击的穿透临时关闭逻辑）。
- 托盘图标与菜单（显示窗口 / 置顶 / 鼠标穿透 / 退出）。
- 主题设置：背景透明度、背景色、强调色，实时预览并持久化到 `config.json`。
- 状态圆点：连接 = 强调色，断开 = 红色。
- 窗口位置记忆：退出保存、启动恢复，防止恢复到已移除显示器。
- 置顶 / 穿透状态持久化，下次启动恢复。
- PyInstaller 打包 + Inno Setup 安装器，支持覆盖安装保留配置、卸载保留 API Key。

### Fixed
- 修复关闭鼠标穿透后重启导致的小部件"变白/透成浅色"问题
  （`apply_click_through(False)` 需保留 `WS_EX_LAYERED` 透明合成位）。
- 修复首次保存 API Key 后面板"回弹"问题（保存后轮询状态再关闭面板）。
- 修复置顶开关被 pywebview 自动复位的问题（改用 `on_top=False` + 手动 Win32 管理）。
