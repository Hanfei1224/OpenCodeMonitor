# Changelog

本项目的所有重要变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.2.0] - 2026-08-15

### Added
- **综合 token 采集装置**：合并两个数据源——opencode 本地库 `opencode.db` + Claude Code 会话记录
  `~/.claude/projects/*.jsonl`（解析 assistant 消息的 usage）。程序未运行时产生的消耗，
  下次启动也能从原始记录**自动补全**；统计口径不再局限于 OpenCode Go 套餐，而是本机全部 token 消耗。
- 用量统计的**月份列表只显示有消耗的月份**（无消耗月份自动隐藏）。
- 用量统计**切月无感加速**：全量采集时一次性把各月聚合缓存进内存，切月 ~0ms，不再每次重扫文件。
- 用量统计**窗口位置记忆**：退出保存、下次打开自动恢复到原位置（含在屏校验）。

### Changed
- 主窗口今日统计改为**右侧竖排**，数值放入统一宽度底色块（整列左对齐、数值居中），
  修复长数值（`300M`/`89.3%`）被裁切的问题。
- 数字缩写规则：万以下原样、5 位起用 `k`、`k` 超 4 位换 `M`、`M` 超 4 位换 `B`。
- 「命中 / 命中率」改名「**缓存 / 缓存率**」（主窗口、统计窗口汇总与悬浮提示统一）。
- 不透明度只作用于窗口基底：移除低透明度（<60%）时给控件临时加底色/内边距的几何变化，
  不再出现控件跳动、按钮变黑、悬停高亮失效、今日数值列错位。
- 用量统计窗口启动即用配置的强调色（启动时注入主题），不再先显示默认色再跳变；
  月份菜单的选中项高亮与对勾也跟随强调色。
- 悬浮提示：缓存不再分读/写（显示单一缓存总数）、命中率→缓存率、数值列左对齐。

## [1.1.3] - 2026-08-14

### Added
- 主窗口环图下方新增**今日 token 统计分块**：今日总 token / 输入 / 输出 / 命中（缓存读取）/ 命中率，
  随每轮刷新更新（数值自动缩写 K/M）。
- footer「上次刷新」改为**可点击下拉**选择刷新间隔（预设 5s / 10s / 30s / 60s），选中即写入
  `config.json` 的 `refresh_seconds` 并立即生效——额度环图与 token 计数按同一间隔刷新。
  默认间隔保持 60s（新装默认值不变）。
- 窗口高度加高以容纳今日分块。
- 新增**每日 token 统计日期图**（独立窗口，默认隐藏）：
  - 读本机 opencode 本地库 `~/.local/share/opencode/opencode.db`，按天统计最近 90 天
    `tokens.total / input / output / cache.read / cache.write` 与**缓存命中率**
    （`cache.read ÷ (cache.read + input)`，按 token 口径）。
  - GitHub 贡献图风格：色块颜色跟随强调色，按当日总 token 连续渐变加深（无数据 = 最淡暗底），
    悬停色块显示当日 总 token / 输入 / 输出 / 缓存(read·write) / 命中率 / 请求数。
  - 托盘菜单新增「显示用量统计」；主窗口右上角新增用量统计按钮（SVG 图标）一键切换。
  - 采集间隔与主磁贴刷新间隔（`refresh_seconds`，默认 60s）一致，每轮全量重扫按天聚合（表很小，无增量漏计风险）。
  - 置顶 / 鼠标穿透与主窗口**共享开关**，同时应用到两个窗口。
- 新增 `stats.html`（日期图窗口），并加入 PyInstaller 打包资源。

## [1.1.2] - 2026-08-14

### Fixed
- 修复**重启电脑后首次启动置顶失效**的问题：冷启动时 WebView2 初始化慢，窗口到
  `on_navigation_start`（`edgechromium.on_navigation_start`）才真正显示，而此前置顶只在
  窗口显示前用原生 `WS_EX_TOPMOST` 位设置，随后被 WinForms 用缓存的托管
  `TopMost=False`（`on_top=False` 导致）覆盖，出现 Win+D 能缩略、其他窗口能盖住的现象。
  - `apply_topmost` 现在同时同步 WinForms 托管属性 `native.TopMost`，托管状态与真实窗口一致，
    后续 Show / Activate / 重建句柄都不会把置顶抹掉。
  - `on_start` 的样式重挂从固定 `sleep(1.5)` 改为等待 `window.events.loaded`
    （页面真正加载完成、窗口稳定显示后），超时 20s 兜底，覆盖冷启动慢初始化场景。

## [1.1.1] - 2026-08-13

### Changed
- 右上角「置顶 / 穿透」由文字标签改为**图标按钮**（内联 SVG：图钉 = 置顶、电脑鼠标 = 穿透），
  开启时变绿、关闭时变灰，保持原有开关状态指示。
- 视觉细节调整：
  - 「重置倒计时 / 上次刷新 / 响应时间」文字颜色改为与「已用金额」一致（统一亮色，去掉已用的透明度差异）。
  - 环中心百分比底色由椭圆改为**正圆形**。
  - 环下方「标签 / 重置 / 已用」三行间距加大，避免底色互相粘连。
  - 上述字段字号各放大一号，窗口高度相应加高，避免底部内容被裁剪。

## [1.1.0] - 2026-08-13

### Added
- 右下角显示接口**响应耗时**（毫秒），每次刷新实时更新（`fetch_usage` 用 `time.monotonic()` 计时）。
- **低透明度文字底色**：背景透明度低于 60% 时，为标题、置顶/穿透标签、环中心百分比、
  标签/重置倒计时/已用金额、底部刷新时间与响应耗时等所有文字控件自动垫半透明深色底色
  （圆角矩形 / 圆形），文字可读且不完全遮挡下层内容。
- 安装包文件名加入版本号，命名规则统一为 `OpencodeMonitor-Setup-<版本号>.exe`
  （版本号以 `installer.iss` 的 `MyAppVersion` 为唯一来源）。
- 新增 `README.md` 与 `CHANGELOG.md`。

### Changed
- 版本号从 `1.0.0` 升级为 `1.1.0`（`installer.iss` + `index.html`）。

### Fixed
- 窗口加入 `WS_EX_TOOLWINDOW` 并从任务栏 / Alt+Tab 隐藏，仅保留托盘图标；
  同时清除 WinForms 自动设置的 `WS_EX_APPWINDOW`（0x40000），避免其抵消工具窗口样式、
  导致窗口仍出现在任务栏（实测 exstyle 0xD00A8 → 0x900A8）。
- 环下方文字改为 `width:fit-content` + 水平居中，使底色紧贴文字、上下左右居中。

## [1.0.0] - 2026-08-12

### Added
- 无边框、置顶、可鼠标穿透的桌面磁贴，基于 pywebview(WebView2) + pystray。
- 三个 SVG 环形图：**5 小时 / 本周 / 本月** 用量百分比（官方接口 `opencode.ai/zen/go/v1/usage`）。
- 每个环显示重置倒计时（rolling 精确到分钟，weekly/monthly 精确到小时）。
- **已用美元换算**：`已用$ = percent/100 × 官方配额`（5h=$12、周=$30、月=$60），
  在环下方以 `已用 $X.XX / $Y` 显示。
- 内嵌 API Key 填写面板：未配置 / Key 无效时自动弹出，保存后立即重连（含输入框可点击的穿透临时关闭逻辑）。
- 托盘图标与菜单（显示窗口 / 置顶 / 鼠标穿透 / 退出）。
- 主题设置：背景透明度、背景色、强调色，实时预览并持久化到 `config.json`。
- 状态圆点：连接 = 强调色，断开 = 红色。
- 窗口位置记忆：退出保存、启动恢复，防止恢复到已移除显示器。
- 置顶 / 穿透状态持久化，下次启动恢复。
- PyInstaller 打包 + Inno Setup 安装器，支持覆盖安装保留配置、卸载保留 API Key。

### Fixed
- 修复关闭鼠标穿透后重启导致的小部件"变白/透成浅色"问题
  （`apply_click_through(False)` 需保留 `WS_EX_LAYERED` 透明合成位）。
- 修复首次保存 API Key 后面板"回弹"问题（保存后轮询状态再关闭面板）。
- 修复置顶开关被 pywebview 自动复位的问题（改用 `on_top=False` + 手动 Win32 管理）。
